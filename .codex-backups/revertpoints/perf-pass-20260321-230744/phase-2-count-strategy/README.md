Phase 2 revert point

Restores the service-layer count strategy before switching heavy paginated list queries away from exact totals.

Restore by copying the backed up files in this folder back to the repo root.
