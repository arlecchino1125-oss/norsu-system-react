import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'edu.norsu.care.student',
  appName: 'NORSU Student Portal',
  webDir: '../../../dist-student',
};

export default config;
